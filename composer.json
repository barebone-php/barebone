{
  "name": "barebone/barebone",
  "description": "MVC Web Framework",
  "keywords": ["framework", "mvc", "barebone"],
  "type": "project",
  "license": "MIT",
  "minimum-stability" : "dev",
  "prefer-stable" : true,
  "scripts": {
    "post-create-project-cmd": [
      "@composer install -d core/",
      "@composer dump-autoload -o -d core/"
    ]
  },
  "require": {
    "php": ">=5.5.0"
  }
}
